import { MoreHorizontal } from "lucide-react"

export default function MoreOptions() {
    return (
        <button className="">
            <p className="flex bg-gray-200 rounded-full p-2 hover:bg-gray-300 text-0.5xs"><MoreHorizontal strokeWidth={1} size={20} /></p>
        </button>
    )
}